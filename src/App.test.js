import { render, screen } from '@testing-library/react';
import App from './App';
import './geolocation.mock'; // Adjust the path as needed
test('renders learn react link', () => {
  render(<App />);
  // Your test assertions go here
});